# Echo Plugin

Minimal process plugin for EmoAgent Plugin Runtime v0.2.

Install this directory through the admin plugin API or UI local-path installer. It registers `after_turn_end`, `echo`, and `provider_ping`.
